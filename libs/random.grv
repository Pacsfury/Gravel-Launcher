package: random

namespace rand
	fun initRNG():
		int seed = 1
	end

	fun randint(int min, int max):
		int module = 65536
		int mul       = 25173
		int increm  = 13849
		
		seed = ((seed * mul) + increm) % module

		return min + (seed ( % (max-min + 1)))
	end
end

